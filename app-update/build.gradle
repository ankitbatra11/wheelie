android {
    defaultConfig {
        minSdkVersion 16
        targetSdkVersion 30
    }
    lintOptions {
        abortOnError false
    }
}

dependencies {
    api project(':core')
    api project(':lifecycle')
    api "com.google.android.play:core:$PLAY_CORE_VERSION"
}